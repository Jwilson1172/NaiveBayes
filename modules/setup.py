import setuptools

setuptools.setup(
    name="NaiveBayes",
    version="0.0.1",
    description="A work around for working from a subdir in a notebook",
    packages=setuptools.find_packages(),
    python_requires='>=3.8',
)
